{
  "name": "money-flow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "node --test",
    "start:api": "node --env-file=.env apps/api/src/server.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {}
}
